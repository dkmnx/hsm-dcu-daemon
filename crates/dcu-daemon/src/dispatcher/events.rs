//! Placeholder for event type definitions.
//! The primary types are defined in [`super::mod`] (`NcpEvent`, `EventDrivenTask`,
//! `TaskProgress`). This file exists for files that logically belong under
//! `dispatcher/` but don't fit in `mod.rs` or `data_pump.rs`.
